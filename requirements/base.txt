scipy
sherpa-onnx==1.10.46
sounddevice
soundfile
# inference_manifest.schema imports jsonschema.protocols, which only exists in
# jsonschema 4.x. The venv enables include-system-site-packages and otherwise
# falls through to the Ubuntu apt package python3-jsonschema 3.2.0, breaking
# ros2 launch with ModuleNotFoundError: No module named 'jsonschema.protocols'.
jsonschema>=4,<5
# inference_manifest models the schema-v2 contract with pydantic v2 (model_validate,
# field_validator, ConfigDict). This is a platform-independent hard dependency of the
# manifest contract, so it is declared once here instead of per-platform, which would
# let Ubuntu and openEuler resolve different contract versions.
pydantic>=2,<3
safetensors>=0.4.3,<1.0.0
