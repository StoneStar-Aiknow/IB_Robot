setuptools>=71,<81
packaging>=24.2,<26
# Must match mujoco_ros2_control's bundled libmujoco.so version (3.4.0).
# scene_compiler uses mujoco Python to compile .mjb with SDF; version mismatch
# causes mj_loadModel to fail at runtime.
mujoco==3.4.0
