diff --git a/CMakeLists.txt b/CMakeLists.txt
index be3040b..d2ca115 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -5,34 +5,39 @@ if(NOT CMAKE_BUILD_TYPE)
   set(CMAKE_BUILD_TYPE Release)
 endif()
 
-ADD_COMPILE_OPTIONS(-std=c++14)
-ADD_COMPILE_OPTIONS(-std=c++14)
-set(CMAKE_CXX_FLAGS "-std=c++14 -O3")
-
+add_compile_options(-O3)
+
 add_definitions(-DROOT_DIR=\"${CMAKE_CURRENT_SOURCE_DIR}/\")
 
 set(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} -fexceptions")
-set(CMAKE_CXX_STANDARD 14)
+set(CMAKE_CXX_STANDARD 17)
 set(CMAKE_CXX_STANDARD_REQUIRED ON)
 set(CMAKE_CXX_EXTENSIONS OFF)
-set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -std=c++14 -pthread -std=c++0x -std=c++14 -fexceptions")
+set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -pthread -fexceptions")
+if(CMAKE_BUILD_TYPE STREQUAL "Debug")
+  set(CMAKE_C_FLAGS_DEBUG "")
+  set(CMAKE_CXX_FLAGS_DEBUG "")
+endif()
+
+add_compile_options($<$<COMPILE_LANGUAGE:CXX>:-Wno-psabi>)
+
 set(CMAKE_POSITION_INDEPENDENT_CODE ON)
 
-message("Current CPU archtecture: ${CMAKE_SYSTEM_PROCESSOR}")
+message(STATUS "Current CPU architecture: ${CMAKE_SYSTEM_PROCESSOR}")
 
 if(CMAKE_SYSTEM_PROCESSOR MATCHES "(x86)|(X86)|(amd64)|(AMD64)")
   include(ProcessorCount)
   ProcessorCount(N)
-  message("Processer number:  ${N}")
+  message(STATUS "Processor count: ${N}")
 
   if(N GREATER 4)
     add_definitions(-DMP_EN)
     add_definitions(-DMP_PROC_NUM=3)
-    message("core for MP: 3")
+    message(STATUS "MP worker count: 3")
   elseif(N GREATER 3)
     add_definitions(-DMP_EN)
     add_definitions(-DMP_PROC_NUM=2)
-    message("core for MP: 2")
+    message(STATUS "MP worker count: 2")
   else()
     add_definitions(-DMP_PROC_NUM=1)
   endif()
@@ -80,7 +85,7 @@ set(dependencies
 find_package(Eigen3 REQUIRED)
 find_package(PCL REQUIRED COMPONENTS common io)
 
-message(Eigen: ${EIGEN3_INCLUDE_DIR})
+message(STATUS "Eigen: ${EIGEN3_INCLUDE_DIR}")
 message(STATUS "PCL: ${PCL_INCLUDE_DIRS}")
 
 set(msg_files
@@ -125,4 +130,4 @@ install(
   DESTINATION share/${PROJECT_NAME}
 )
 
-ament_package()
\ No newline at end of file
+ament_package()
