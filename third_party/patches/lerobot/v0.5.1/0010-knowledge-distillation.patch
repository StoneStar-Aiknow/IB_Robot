From 5da7c023b71e5f88e4a0b2445df956d8f2baafc0 Mon Sep 17 00:00:00 2001
From: XiaoqiangWu <wuxiaoqiang.rtos@huawei.com>
Date: Fri, 24 Apr 2026 22:41:21 +0800
Subject: [PATCH] feature: add knowledge distillation

---
 src/lerobot/policies/act/configuration_act.py |   5 +
 src/lerobot/policies/act/modeling_act.py      | 135 +++++++++----
 .../diffusion/configuration_diffusion.py      |   6 +
 .../policies/diffusion/modeling_diffusion.py  |  65 ++++++-
 .../policies/pi05/configuration_pi05.py       |  34 ++++
 src/lerobot/policies/pi05/modeling_pi05.py    | 170 +++++++++++++++--
 src/lerobot/scripts/knowledge_distillation.py |  73 +++++++
 src/lerobot/scripts/lerobot_train.py          | 179 ++++++++++++++----
 8 files changed, 569 insertions(+), 98 deletions(-)
 create mode 100644 src/lerobot/scripts/knowledge_distillation.py

diff --git a/src/lerobot/policies/act/configuration_act.py b/src/lerobot/policies/act/configuration_act.py
index 6f6c1c4b..2eda7c82 100644
--- a/src/lerobot/policies/act/configuration_act.py
+++ b/src/lerobot/policies/act/configuration_act.py
@@ -124,6 +124,11 @@ class ACTConfig(PreTrainedConfig):
     latent_dim: int = 32
     n_vae_encoder_layers: int = 4
 
+    # Knowledge distillation from a pretrained model.
+    kd: bool = False  # Whether to use knowledge distillation during training.
+    teacher_train_config: str | None = None  # Path to the teacher model's train_config.json
+    decoder_out_dim: int = 1024  # Dimension of the decoder output used in the distillation.
+
     # Inference.
     # Note: the value used in ACT when temporal ensembling is enabled is 0.01.
     temporal_ensemble_coeff: float | None = None
diff --git a/src/lerobot/policies/act/modeling_act.py b/src/lerobot/policies/act/modeling_act.py
index 76bb220b..9cfec54b 100644
--- a/src/lerobot/policies/act/modeling_act.py
+++ b/src/lerobot/policies/act/modeling_act.py
@@ -24,7 +24,6 @@ import os
 from collections import deque
 from collections.abc import Callable
 from itertools import chain
-from typing import Optional
 
 import cv2
 import einops
@@ -35,13 +34,11 @@ import torchvision
 from torch import Tensor, nn
 from torchvision.models._utils import IntermediateLayerGetter
 from torchvision.ops.misc import FrozenBatchNorm2d
+
 from lerobot.policies.act.configuration_act import ACTConfig
 from lerobot.policies.pretrained import PreTrainedPolicy
+from lerobot.utils.attention_masking import create_interactive_masks, process_pixel_mask_to_feature_mask
 from lerobot.utils.constants import ACTION, OBS_ENV_STATE, OBS_IMAGES, OBS_STATE
-from lerobot.utils.attention_masking import (
-    create_interactive_masks,
-    process_pixel_mask_to_feature_mask
-)
 from lerobot.utils.visualization import RealTimeVisualizer, visualize_attention_maps
 
 
@@ -87,13 +84,19 @@ class ACTPolicy(PreTrainedPolicy):
         self.gui_save_dir = "gui_interactions"
 
         if self.config.is_ascend_om_enabled:
-            print(f"[INFO] Ascend om enabled, training and visualization are not allowed")
+            print("[INFO] Ascend om enabled, training and visualization are not allowed")
             self.enable_interactive_masking = False
             self.enable_visualization = False
             from lerobot.oee.ascend.om.ACTWrapper import ACTWrapper
+
             # use precompiled kernels to speed up warm up phase
             self.om_model = ACTWrapper(self.config.om_model_path, self.config)
             torch.npu.set_compile_mode(jit_compile=False)
+        elif self.config.is_ascend_om_3403_enabled:
+            print("[INFO] Ascend om of SD3403 enabled, training and visualization are not allowed")
+            from lerobot.oee.ascend.om.ACTWrapper_3403 import ACT3403Policy
+
+            self.om_model = ACT3403Policy(self.config.om_model_path)
         else:
             # only init original act model here to speed up warm up phase in om mode
             self.model = ACT(config)
@@ -103,7 +106,7 @@ class ACTPolicy(PreTrainedPolicy):
 
         # RealTimeVisualizer初始化
         self.visualizer = None
-        if os.environ.get('VIZ_MODE', 'file').lower() == 'interactive':
+        if os.environ.get("VIZ_MODE", "file").lower() == "interactive":
             self.visualization_frequency = 1
             print("--- INFO: Real-time interactive visualization mode is ENABLED. ---")
             if self.config.image_features:
@@ -116,7 +119,9 @@ class ACTPolicy(PreTrainedPolicy):
                     blend_alpha=self.viz_blend_alpha,
                 )
             else:
-                print("--- WARNING: Interactive visualization enabled, but no image features found. Disabling. ---")
+                print(
+                    "--- WARNING: Interactive visualization enabled, but no image features found. Disabling. ---"
+                )
         self.feature_map_size = None
         self.reset()
 
@@ -191,7 +196,7 @@ class ACTPolicy(PreTrainedPolicy):
         Return whether the next select_action needs new observations.
         For ACT, as long as there are actions in the action queue, new observations are not needed.
         """
-        return len(cls._action_queue) == 0 if hasattr(cls, '_action_queue') else True
+        return len(cls._action_queue) == 0 if hasattr(cls, "_action_queue") else True
 
     def _visualize_if_enabled(self, batch):
         if not self.enable_visualization or not self.config.image_features:
@@ -201,13 +206,16 @@ class ACTPolicy(PreTrainedPolicy):
             return
 
         # 频率控制 (如果是交互模式，visualization_frequency 应该是 1)
-        if self.visualization_frequency > 0 and self.inference_step_counter % self.visualization_frequency != 0:
+        if (
+            self.visualization_frequency > 0
+            and self.inference_step_counter % self.visualization_frequency != 0
+        ):
             return
 
         # 准备实时图像
         original_images_for_viz = {}
         # 注意：这里的 batch 是环境传进来的最新实时数据！
-        for cam_key in self.config.image_features.keys():
+        for cam_key in self.config.image_features:
             img_tensor = batch[cam_key][0].cpu() * 255.0
             img_np = img_tensor.permute(1, 2, 0).numpy().astype(np.uint8)
             original_images_for_viz[cam_key] = cv2.cvtColor(img_np, cv2.COLOR_RGB2BGR)
@@ -235,8 +243,11 @@ class ACTPolicy(PreTrainedPolicy):
         self.eval()
         self.inference_step_counter += 1
 
-        if ((self.enable_visualization or self.enable_interactive_masking)
-                and self.feature_map_size is None and self.config.image_features):
+        if (
+            (self.enable_visualization or self.enable_interactive_masking)
+            and self.feature_map_size is None
+            and self.config.image_features
+        ):
             first_cam_key = list(self.config.image_features.keys())[0]
 
             # 使用已经归一化过的batch输入到backbone
@@ -254,8 +265,7 @@ class ACTPolicy(PreTrainedPolicy):
             if pixel_masks:
                 for cam_key, mask_gray in pixel_masks.items():
                     feature_mask = process_pixel_mask_to_feature_mask(
-                        mask_image=mask_gray,
-                        feature_map_size=self.feature_map_size
+                        mask_image=mask_gray, feature_map_size=self.feature_map_size
                     )
                     if feature_mask is not None:
                         self.user_attention_masks[cam_key] = feature_mask
@@ -267,16 +277,21 @@ class ACTPolicy(PreTrainedPolicy):
         if self.user_attention_masks and self.config.image_features and self.feature_map_size:
             feature_map_area = self.feature_map_size[0] * self.feature_map_size[1]
             all_cam_masks = []
-            for cam_key in self.config.image_features.keys():
+            for cam_key in self.config.image_features:
                 mask_device = next(self.model.parameters()).device
                 if cam_key in self.user_attention_masks:
                     all_cam_masks.append(self.user_attention_masks[cam_key].to(mask_device))
                 else:
                     all_cam_masks.append(torch.zeros(feature_map_area, dtype=torch.bool, device=mask_device))
             concatenated_visual_mask = torch.cat(all_cam_masks)
-            num_non_image_tokens = 1 + (1 if self.config.robot_state_feature else 0) + (
-                1 if self.config.env_state_feature else 0)
-            non_image_mask = torch.zeros(num_non_image_tokens, dtype=torch.bool, device=concatenated_visual_mask.device)
+            num_non_image_tokens = (
+                1
+                + (1 if self.config.robot_state_feature else 0)
+                + (1 if self.config.env_state_feature else 0)
+            )
+            non_image_mask = torch.zeros(
+                num_non_image_tokens, dtype=torch.bool, device=concatenated_visual_mask.device
+            )
             mask_1d = torch.cat([non_image_mask, concatenated_visual_mask])
             final_attention_mask = mask_1d.unsqueeze(0).expand(self.config.chunk_size, -1)
             batch_size = batch[OBS_STATE].shape[0]
@@ -287,33 +302,50 @@ class ACTPolicy(PreTrainedPolicy):
             batch = dict(batch)  # shallow copy so that adding a key doesn't modify the original
             batch[OBS_IMAGES] = [batch[key] for key in self.config.image_features]
 
-        if self.config.is_ascend_om_enabled:
+        if self.config.is_ascend_om_enabled or self.config.is_ascend_om_3403_enabled:
             actions = self.om_model.predict(batch)[0]
         else:
-            actions, _, attn_weights = self.model(
+            actions, _, attn_weights, _ = self.model(
                 batch,
                 attention_mask=final_attention_mask,
-                encoder_key_padding_mask=final_encoder_key_padding_mask  # <-- 传入新掩码
+                encoder_key_padding_mask=final_encoder_key_padding_mask,  # <-- 传入新掩码
             )
         if self.enable_visualization and attn_weights is not None:
             self.last_attn_weights = attn_weights
 
         return actions
 
-    def forward(self, batch: dict[str, Tensor]) -> tuple[Tensor, dict]:
+    def compute_kd_loss(self, batch, actions_hat, decoder_out, teacher_policy, loss_dict):
+        teacher_action_hat, (teacher_mu, teacher_log_sigma_x2), _, teacher_decoder_out = teacher_policy.model(
+            batch
+        )
+
+        # 计算差异
+        kd_loss = nn.MSELoss()(decoder_out, teacher_decoder_out)
+
+        # 总蒸馏损失
+        loss_dict["kd_loss"] = kd_loss.item()
+        return loss_dict
+
+    # 增加teacher_policy实现蒸馏学习，并记录蒸馏损失
+    def forward(
+        self, batch: dict[str, Tensor], teacher_policy=None, teacher_feature=None
+    ) -> tuple[Tensor, dict]:
         """Run the batch through the model and compute the loss for training or validation."""
         if self.config.image_features:
             batch = dict(batch)  # shallow copy so that adding a key doesn't modify the original
             batch[OBS_IMAGES] = [batch[key] for key in self.config.image_features]
 
-        actions_hat, (mu_hat, log_sigma_x2_hat), _ = self.model(batch)
+        actions_hat, (mu_hat, log_sigma_x2_hat), _, decoder_out = self.model(batch)
 
         l1_loss = (
             F.l1_loss(batch[ACTION], actions_hat, reduction="none") * ~batch["action_is_pad"].unsqueeze(-1)
         ).mean()
 
         loss_dict = {"l1_loss": l1_loss.item()}
-        loss_dict["loss"] = F.l1_loss(batch["action"], actions_hat, reduction="none") * ~batch["action_is_pad"].unsqueeze(-1)
+        loss_dict["loss"] = F.l1_loss(batch["action"], actions_hat, reduction="none") * ~batch[
+            "action_is_pad"
+        ].unsqueeze(-1)
         if self.config.use_vae and self.training:
             # Calculate Dₖₗ(latent_pdf || standard_normal). Note: After computing the KL-divergence for
             # each dimension independently, we sum over the latent dimension to get the total
@@ -327,6 +359,12 @@ class ACTPolicy(PreTrainedPolicy):
         else:
             loss = l1_loss
 
+        # 知识蒸馏损失计算
+        if teacher_policy is not None and self.training:
+            loss_dict = self.compute_kd_loss(batch, actions_hat, decoder_out, teacher_policy, loss_dict)
+
+        loss_dict["total_kd_loss"] = loss + loss_dict.get("kd_loss", 0.0)
+
         return loss, loss_dict
 
 
@@ -535,6 +573,12 @@ class ACT(nn.Module):
         # Final action regression head on the output of the transformer's decoder.
         self.action_head = nn.Linear(config.dim_model, self.config.action_feature.shape[0])
 
+        # 增加蒸馏学习层，如需蒸馏学习，则保证输出的两层为同一维度，需要做修改
+        print("是否蒸馏学习: ", self.config.kd)
+        if self.config.kd:
+            self.trans = nn.Linear(self.config.dim_model, self.config.decoder_out_dim)
+            self.action_head = nn.Linear(self.config.decoder_out_dim, self.config.action_feature.shape[0])
+
         self._reset_parameters()
 
     def _reset_parameters(self):
@@ -544,11 +588,11 @@ class ACT(nn.Module):
                 nn.init.xavier_uniform_(p)
 
     def forward(
-            self,
-            batch: dict[str, Tensor],
-            attention_mask: Optional[Tensor] = None,
-            encoder_key_padding_mask: Optional[Tensor] = None
-    ) -> tuple[Tensor, tuple[Tensor, Tensor] | tuple[None, None], Optional[Tensor]]:
+        self,
+        batch: dict[str, Tensor],
+        attention_mask: Tensor | None = None,
+        encoder_key_padding_mask: Tensor | None = None,
+    ) -> tuple[Tensor, tuple[Tensor, Tensor] | tuple[None, None], Tensor | None]:
         """A forward pass through the Action Chunking Transformer (with optional VAE encoder).
 
         `batch` should have the following structure:
@@ -616,7 +660,7 @@ class ACT(nn.Module):
             latent_pdf_params = self.vae_encoder_latent_output_proj(cls_token_out)
             mu = latent_pdf_params[:, : self.config.latent_dim]
             # This is 2log(sigma). Done this way to match the original implementation.
-            log_sigma_x2 = latent_pdf_params[:, self.config.latent_dim:]
+            log_sigma_x2 = latent_pdf_params[:, self.config.latent_dim :]
 
             # Sample the latent with the reparameterization trick.
             latent_sample = mu + log_sigma_x2.div(2).exp() * torch.randn_like(mu)
@@ -661,8 +705,9 @@ class ACT(nn.Module):
         encoder_in_pos_embed = torch.stack(encoder_in_pos_embed, axis=0)
 
         # Forward pass through the transformer modules.
-        encoder_out = self.encoder(encoder_in_tokens, pos_embed=encoder_in_pos_embed,
-                                   key_padding_mask=encoder_key_padding_mask)
+        encoder_out = self.encoder(
+            encoder_in_tokens, pos_embed=encoder_in_pos_embed, key_padding_mask=encoder_key_padding_mask
+        )
         # TODO(rcadene, alexander-soare): remove call to `device` ; precompute and use buffer
         decoder_in = torch.zeros(
             (self.config.chunk_size, batch_size, self.config.dim_model),
@@ -680,9 +725,13 @@ class ACT(nn.Module):
         # Move back to (B, S, C).
         decoder_out = decoder_out.transpose(0, 1)
 
+        # 增加蒸馏学习层
+        if self.config.kd:
+            decoder_out = self.trans(decoder_out)
+
         actions = self.action_head(decoder_out)
 
-        return actions, (mu, log_sigma_x2), all_cross_attn_weights
+        return actions, (mu, log_sigma_x2), all_cross_attn_weights, decoder_out
 
 
 class ACTEncoder(nn.Module):
@@ -756,13 +805,16 @@ class ACTDecoder(nn.Module):
         encoder_out: Tensor,
         decoder_pos_embed: Tensor | None = None,
         encoder_pos_embed: Tensor | None = None,
-        attention_mask: Optional[Tensor] = None,
+        attention_mask: Tensor | None = None,
     ) -> tuple[Tensor, Tensor]:
         all_cross_attn_weights = []
         for layer in self.layers:
             x, cross_attn_weights = layer(
-                x, encoder_out, decoder_pos_embed=decoder_pos_embed, encoder_pos_embed=encoder_pos_embed,
-                attention_mask=attention_mask
+                x,
+                encoder_out,
+                decoder_pos_embed=decoder_pos_embed,
+                encoder_pos_embed=encoder_pos_embed,
+                attention_mask=attention_mask,
             )
             all_cross_attn_weights.append(cross_attn_weights)
         if self.norm is not None:
@@ -774,8 +826,9 @@ class ACTDecoderLayer(nn.Module):
     def __init__(self, config: ACTConfig):
         super().__init__()
         self.self_attn = nn.MultiheadAttention(config.dim_model, config.n_heads, dropout=config.dropout)
-        self.multihead_attn = nn.MultiheadAttention(config.dim_model, config.n_heads, dropout=config.dropout,
-                                                    batch_first=False)
+        self.multihead_attn = nn.MultiheadAttention(
+            config.dim_model, config.n_heads, dropout=config.dropout, batch_first=False
+        )
 
         # Feed forward layers.
         self.linear1 = nn.Linear(config.dim_model, config.dim_feedforward)
@@ -801,7 +854,7 @@ class ACTDecoderLayer(nn.Module):
         encoder_out: Tensor,
         decoder_pos_embed: Tensor | None = None,
         encoder_pos_embed: Tensor | None = None,
-        attention_mask: Optional[Tensor] = None
+        attention_mask: Tensor | None = None,
     ) -> tuple[Tensor, Tensor]:
         """
         Args:
@@ -831,7 +884,7 @@ class ACTDecoderLayer(nn.Module):
             value=encoder_out,
             attn_mask=attention_mask,
             need_weights=True,
-            average_attn_weights=False
+            average_attn_weights=False,
         )
         x = skip + self.dropout2(cross_attn_out)
         if self.pre_norm:
diff --git a/src/lerobot/policies/diffusion/configuration_diffusion.py b/src/lerobot/policies/diffusion/configuration_diffusion.py
index 54569434..43326c3d 100644
--- a/src/lerobot/policies/diffusion/configuration_diffusion.py
+++ b/src/lerobot/policies/diffusion/configuration_diffusion.py
@@ -145,6 +145,12 @@ class DiffusionConfig(PreTrainedConfig):
     clip_sample: bool = True
     clip_sample_range: float = 1.0
 
+    # Knowledge distillation from a pretrained model.
+    teacher_policy: bool = False  # Whether as a teacher policy.
+    kd: bool = False  # Whether to use knowledge distillation during training.
+    teacher_train_config: str | None = None  # Path to the teacher model's train_config.json
+    decoder_out_dim: int = 1024  # Dimension of the decoder output used in the distillation.
+
     # Inference
     num_inference_steps: int | None = None
 
diff --git a/src/lerobot/policies/diffusion/modeling_diffusion.py b/src/lerobot/policies/diffusion/modeling_diffusion.py
index 3ab6719c..9ecacc54 100644
--- a/src/lerobot/policies/diffusion/modeling_diffusion.py
+++ b/src/lerobot/policies/diffusion/modeling_diffusion.py
@@ -137,14 +137,19 @@ class DiffusionPolicy(PreTrainedPolicy):
         action = self._queues[ACTION].popleft()
         return action
 
-    def forward(self, batch: dict[str, Tensor]) -> tuple[Tensor, None]:
+    def forward(
+        self, batch: dict[str, Tensor], teacher_policy=None, teacher_feature=None, get_feature=False
+    ) -> tuple[Tensor, None]:
         """Run the batch through the model and compute the loss for training or validation."""
         if self.config.image_features:
             batch = dict(batch)  # shallow copy so that adding a key doesn't modify the original
             batch[OBS_IMAGES] = torch.stack([batch[key] for key in self.config.image_features], dim=-4)
-        loss = self.diffusion.compute_loss(batch)
+        if get_feature:
+            kd_features = self.diffusion.compute_loss(batch, get_feature=get_feature)
+            return kd_features
+        loss, loss_dict = self.diffusion.compute_loss(batch, teacher_feature=teacher_feature)
         # no output_dict so returning None
-        return loss, None
+        return loss, loss_dict
 
 
 def _make_noise_scheduler(name: str, **kwargs: dict) -> DDPMScheduler | DDIMScheduler:
@@ -224,7 +229,7 @@ class DiffusionModel(nn.Module):
 
         for t in self.noise_scheduler.timesteps:
             # Predict model output.
-            model_output = self.unet(
+            model_output, _ = self.unet(
                 sample,
                 torch.full(sample.shape[:1], t, dtype=torch.long, device=sample.device),
                 global_cond=global_cond,
@@ -299,7 +304,16 @@ class DiffusionModel(nn.Module):
 
         return actions
 
-    def compute_loss(self, batch: dict[str, Tensor]) -> Tensor:
+    def compute_kd_loss(self, kd_features, teacher_kd_features, loss_dict):
+        # 计算差异
+        kd_loss = nn.MSELoss()(kd_features, teacher_kd_features)
+        # 总蒸馏损失
+        loss_dict["kd_loss"] = kd_loss.item()
+        return loss_dict
+
+    def compute_loss(
+        self, batch: dict[str, Tensor], teacher_policy=None, teacher_feature=None, get_feature=False
+    ) -> Tensor:
         """
         This function expects `batch` to have (at least):
         {
@@ -338,8 +352,12 @@ class DiffusionModel(nn.Module):
         # Add noise to the clean trajectories according to the noise magnitude at each timestep.
         noisy_trajectory = self.noise_scheduler.add_noise(trajectory, eps, timesteps)
 
+        if get_feature:
+            _, kd_features = self.unet(noisy_trajectory, timesteps, global_cond=global_cond)
+            return kd_features
+
         # Run the denoising network (that might denoise the trajectory, or attempt to predict the noise).
-        pred = self.unet(noisy_trajectory, timesteps, global_cond=global_cond)
+        pred, kd_features = self.unet(noisy_trajectory, timesteps, global_cond=global_cond)
 
         # Compute the loss.
         # The target is either the original trajectory, or the noise.
@@ -350,6 +368,8 @@ class DiffusionModel(nn.Module):
         else:
             raise ValueError(f"Unsupported prediction type {self.config.prediction_type}")
 
+        loss_dict = {}
+
         loss = F.mse_loss(pred, target, reduction="none")
 
         # Mask loss wherever the action is padded with copies (edges of the dataset trajectory).
@@ -362,7 +382,13 @@ class DiffusionModel(nn.Module):
             in_episode_bound = ~batch["action_is_pad"]
             loss = loss * in_episode_bound.unsqueeze(-1)
 
-        return loss.mean()
+        loss_dict["loss"] = loss
+        if teacher_feature is not None and self.training:
+            loss_dict = self.compute_kd_loss(kd_features, teacher_feature, loss_dict)
+
+        loss_dict["total_kd_loss"] = loss.mean() + loss_dict.get("kd_loss", 0.0)
+
+        return loss.mean(), loss_dict
 
 
 class SpatialSoftmax(nn.Module):
@@ -660,6 +686,18 @@ class DiffusionConditionalUnet1d(nn.Module):
             nn.Conv1d(config.down_dims[0], config.action_feature.shape[0], 1),
         )
 
+        # ========== 仅新增：对齐层 + 恢复层（极简版） ==========
+        # 对齐层：学生维度[1024,32] → 教师维度[2048,16]
+        self.align_layer = nn.Sequential(
+            nn.Conv1d(1024, 2048, kernel_size=1),  # 通道数1024→2048
+            nn.GroupNorm(config.n_groups, 2048),
+        )
+        # 恢复层：教师维度[2048,16] → 学生维度[1024,32]
+        self.recover_layer = nn.Sequential(
+            nn.ConvTranspose1d(2048, 1024, kernel_size=1),  # 时序16→32
+            nn.GroupNorm(config.n_groups, 1024),
+        )
+
     def forward(self, x: Tensor, timestep: Tensor | int, global_cond=None) -> Tensor:
         """
         Args:
@@ -692,6 +730,17 @@ class DiffusionConditionalUnet1d(nn.Module):
         for mid_module in self.mid_modules:
             x = mid_module(x, global_feature)
 
+        if self.config.kd:
+            # ========== 核心：对齐层 + 恢复层（极简逻辑） ==========
+            # 1. 保存原始瓶颈特征（学生维度[8,1024,32]）
+            student_feat = x.clone()
+            # 2. 对齐到教师维度[8,2048,16]（用于蒸馏）
+            kd_features = self.align_layer(x)
+            # 3. 恢复回学生维度[8,1024,32]（不影响后续解码器）
+            x = self.recover_layer(kd_features) + student_feat  # 残差连接，保留原有特征
+        else:
+            kd_features = x
+
         # Run decoder, using the skip features from the encoder.
         for resnet, resnet2, upsample in self.up_modules:
             x = torch.cat((x, encoder_skip_features.pop()), dim=1)
@@ -702,7 +751,7 @@ class DiffusionConditionalUnet1d(nn.Module):
         x = self.final_conv(x)
 
         x = einops.rearrange(x, "b d t -> b t d")
-        return x
+        return x, kd_features
 
 
 class DiffusionConditionalResidualBlock1d(nn.Module):
diff --git a/src/lerobot/policies/pi05/configuration_pi05.py b/src/lerobot/policies/pi05/configuration_pi05.py
index 61346c33..2f569f30 100644
--- a/src/lerobot/policies/pi05/configuration_pi05.py
+++ b/src/lerobot/policies/pi05/configuration_pi05.py
@@ -51,6 +51,9 @@ class PI05Config(PreTrainedConfig):
     # Add empty images. Used to add empty cameras when no image features are present.
     empty_cameras: int = 0
 
+    # Parameter freezing configuration
+    frozen_components: list[str] | None = field(default_factory=list)  # 冻结组件列表
+
     tokenizer_max_length: int = 200  # see openpi `__post_init__`
 
     normalization_mapping: dict[str, NormalizationMode] = field(
@@ -83,6 +86,21 @@ class PI05Config(PreTrainedConfig):
 
     tokenizer_max_length: int = 200  # see openpi `__post_init__`
 
+    # LoRA configuration
+    use_lora: bool = field(default=False, metadata={"help": "Whether to use LoRA fine-tuning"})
+    lora_r: int = field(default=8, metadata={"help": "LoRA rank"})
+    lora_alpha: int = field(default=16, metadata={"help": "LoRA alpha parameter"})
+    lora_dropout: float = field(default=0.1, metadata={"help": "LoRA dropout rate"})
+    lora_target_modules: list[str] = field(
+        default_factory=lambda: ["q_proj", "v_proj"], metadata={"help": "Target modules to apply LoRA to"}
+    )
+    lora_freeze_base: bool = field(default=True, metadata={"help": "Whether to freeze base model parameters"})
+
+    # Knowledge distillation from a pretrained model.
+    kd: bool = False  # Whether to use knowledge distillation during training.
+    teacher_train_config: str | None = None  # Path to the teacher model's train_config.json
+    decoder_out_dim: int = 1024  # Dimension of the decoder output used in the distillation.
+
     def __post_init__(self):
         super().__post_init__()
 
@@ -101,6 +119,22 @@ class PI05Config(PreTrainedConfig):
         if self.dtype not in ["bfloat16", "float32"]:
             raise ValueError(f"Invalid dtype: {self.dtype}")
 
+        # Validate frozen_components
+        valid_components = [
+            "vision_tower",
+            "language_model",
+            "action_expert",
+            "action_proj",
+            "time_mlp",
+            "all_visual",
+            "all_text",
+        ]
+        for component in self.frozen_components:
+            if component not in valid_components:
+                raise ValueError(
+                    f"Invalid frozen component: {component}. Valid options are: {valid_components}"
+                )
+
     def validate_features(self) -> None:
         """Validate and set up input/output features."""
         for i in range(self.empty_cameras):
diff --git a/src/lerobot/policies/pi05/modeling_pi05.py b/src/lerobot/policies/pi05/modeling_pi05.py
index 93ca5fa8..f01b28f1 100644
--- a/src/lerobot/policies/pi05/modeling_pi05.py
+++ b/src/lerobot/policies/pi05/modeling_pi05.py
@@ -19,7 +19,7 @@ import logging
 import math
 from collections import deque
 from pathlib import Path
-from typing import TYPE_CHECKING, Literal
+from typing import TYPE_CHECKING, Any, Literal
 
 import torch
 import torch.nn.functional as F  # noqa: N812
@@ -50,6 +50,38 @@ from lerobot.utils.constants import (
 )
 
 
+# LoRA Implementation
+class LoRALayer(nn.Module):
+    def __init__(self, in_dim: int, out_dim: int, rank: int, alpha: float = 1.0, dropout: float = 0.0):
+        super().__init__()
+        self.rank = rank
+        self.alpha = alpha
+        self.dropout = nn.Dropout(dropout) if dropout > 0 else nn.Identity()
+
+        # LoRA matrices
+        self.lora_A = nn.Linear(in_dim, rank, bias=False)
+        self.lora_B = nn.Linear(rank, out_dim, bias=False)
+
+        # Initialize LoRA weights
+        nn.init.kaiming_uniform_(self.lora_A.weight, a=math.sqrt(5))
+        nn.init.zeros_(self.lora_B.weight)
+
+    def forward(self, x: Tensor) -> Tensor:
+        x = self.dropout(x)
+        lora_output = self.lora_B(self.lora_A(x))
+        return self.alpha * lora_output
+
+
+class LoRALinear(nn.Module):
+    def __init__(self, linear_layer: nn.Linear, rank: int, alpha: float = 1.0, dropout: float = 0.0):
+        super().__init__()
+        self.linear = linear_layer
+        self.lora = LoRALayer(linear_layer.in_features, linear_layer.out_features, rank, alpha, dropout)
+
+    def forward(self, x: Tensor) -> Tensor:
+        return self.linear(x) + self.lora(x)
+
+
 def get_safe_dtype(target_dtype, device_type):
     """Get a safe dtype for the given device type."""
     if device_type == "mps" and target_dtype == torch.float64:
@@ -328,6 +360,7 @@ class PaliGemmaWithExpertModel(
         action_expert_config,
         use_adarms=None,
         precision: Literal["bfloat16", "float32"] = "bfloat16",
+        lora_config: dict[str, Any] | None = None,
     ):
         if use_adarms is None:
             use_adarms = [False, False]
@@ -370,8 +403,59 @@ class PaliGemmaWithExpertModel(
         self.gemma_expert = GemmaForCausalLM(config=action_expert_config_hf)
         self.gemma_expert.model.embed_tokens = None
 
+        # Store LoRA configuration
+        self.lora_config = lora_config
+
+        # Apply LoRA if configured
+        if lora_config and lora_config.get("use_lora", False):
+            self._apply_lora()
+
         self.to_bfloat16_for_selected_params(precision)
 
+    def _apply_lora(self):
+        """Apply LoRA to the model based on configuration."""
+        lora_r = self.lora_config.get("lora_r", 8)
+        lora_alpha = self.lora_config.get("lora_alpha", 16)
+        lora_dropout = self.lora_config.get("lora_dropout", 0.1)
+        target_modules = self.lora_config.get("target_modules", ["q_proj", "v_proj"])
+
+        logging.info(
+            f"Applying LoRA with r={lora_r}, alpha={lora_alpha}, dropout={lora_dropout}, target_modules={target_modules}"
+        )
+
+        total_lora_layers = 0
+
+        # Apply LoRA to PaliGemma language model
+        paligemma_layers = self._apply_lora_to_model(
+            self.paligemma.language_model, lora_r, lora_alpha, lora_dropout, target_modules
+        )
+        total_lora_layers += paligemma_layers
+
+        # Apply LoRA to Gemma expert model
+        gemma_expert_layers = self._apply_lora_to_model(
+            self.gemma_expert.model, lora_r, lora_alpha, lora_dropout, target_modules
+        )
+        total_lora_layers += gemma_expert_layers
+
+        logging.info(f"Applied LoRA to {total_lora_layers} layers in total")
+
+    def _apply_lora_to_model(self, model, lora_r, lora_alpha, lora_dropout, target_modules):
+        """Apply LoRA to a specific model component."""
+        lora_applied_count = 0
+        for name, module in model.named_children():
+            if isinstance(module, nn.Linear) and any(target in name for target in target_modules):
+                # Replace the linear layer with LoRALinear
+                lora_linear = LoRALinear(module, lora_r, lora_alpha, lora_dropout)
+                setattr(model, name, lora_linear)
+                lora_applied_count += 1
+            else:
+                # Recursively apply to child modules
+                lora_applied_count += self._apply_lora_to_model(
+                    module, lora_r, lora_alpha, lora_dropout, target_modules
+                )
+
+        return lora_applied_count
+
     def to_bfloat16_for_selected_params(self, precision: Literal["bfloat16", "float32"] = "bfloat16"):
         if precision == "bfloat16":
             self.to(dtype=torch.bfloat16)
@@ -509,13 +593,25 @@ class PI05Pytorch(nn.Module):  # see openpi `PI0Pytorch`
         paligemma_config = get_gemma_config(config.paligemma_variant)
         action_expert_config = get_gemma_config(config.action_expert_variant)
 
+        # Extract LoRA configuration from main config
+        lora_config = {
+            "use_lora": getattr(config, "use_lora", False),
+            "lora_r": getattr(config, "lora_r", 8),
+            "lora_alpha": getattr(config, "lora_alpha", 16),
+            "lora_dropout": getattr(config, "lora_dropout", 0.1),
+            "target_modules": getattr(config, "lora_target_modules", ["q_proj", "v_proj"]),
+        }
+
         self.paligemma_with_expert = PaliGemmaWithExpertModel(
             paligemma_config,
             action_expert_config,
             use_adarms=[False, True],
             precision=config.dtype,
+            lora_config=lora_config,
         )
 
+        self.kd = getattr(config, "kd", False)
+
         self.action_in_proj = nn.Linear(config.max_action_dim, action_expert_config.width)
         self.action_out_proj = nn.Linear(action_expert_config.width, config.max_action_dim)
 
@@ -690,10 +786,18 @@ class PI05Pytorch(nn.Module):  # see openpi `PI0Pytorch`
         prefix_embs, prefix_pad_masks, prefix_att_masks = self.embed_prefix(images, img_masks, tokens, masks)
         suffix_embs, suffix_pad_masks, suffix_att_masks, adarms_cond = self.embed_suffix(x_t, time)
 
-        if (
-            self.paligemma_with_expert.paligemma.language_model.layers[0].self_attn.q_proj.weight.dtype
-            == torch.bfloat16
-        ):
+        # 获取 q_proj 层
+        q_proj_layer = self.paligemma_with_expert.paligemma.language_model.layers[0].self_attn.q_proj
+
+        # 检查是否是 LoRA 层并获取数据类型
+        if hasattr(q_proj_layer, "linear"):
+            # 如果是 LoRA 层，从基础层获取权重
+            weight_dtype = q_proj_layer.linear.weight.dtype
+        else:
+            # 如果是普通线性层，直接获取权重
+            weight_dtype = q_proj_layer.weight.dtype
+
+        if weight_dtype == torch.bfloat16:
             suffix_embs = suffix_embs.to(dtype=torch.bfloat16)
             prefix_embs = prefix_embs.to(dtype=torch.bfloat16)
 
@@ -838,17 +942,50 @@ class PI05Policy(PreTrainedPolicy):
         config.validate_features()
         self.config = config
 
-        # Initialize the core PI05 model
-        self.model = PI05Pytorch(config)
+        # kd属性
+        self.kd = getattr(config, "kd", False)
+        logging.info(f"Knowledge distillation (kd) is set to: {self.kd}")
+
+        torch.cuda.empty_cache()
+
+        with torch.device("cpu"):
+            self.model = PI05Pytorch(config)
+
+        if config.device and config.device != "cpu":
+            self.model = self.model.to(config.device)
 
-        # Enable gradient checkpointing if requested
         if config.gradient_checkpointing:
             self.model.gradient_checkpointing_enable()
 
+        # Freeze base model parameters if using LoRA
+        if getattr(config, "use_lora", False) and getattr(config, "lora_freeze_base", True):
+            self._freeze_base_model_for_lora()
+
         self.model.to(config.device)
 
         self.reset()
 
+    def _freeze_base_model_for_lora(self):
+        """Freeze base model parameters and only train LoRA parameters."""
+        logging.info("Freezing base model parameters for LoRA training")
+
+        # Freeze all parameters first
+        for param in self.model.parameters():
+            param.requires_grad = False
+
+        # Unfreeze LoRA parameters and projection layers
+        for name, param in self.model.named_parameters():
+            if any(keyword in name for keyword in ["lora", "action_in_proj", "action_out_proj", "time_mlp"]):
+                param.requires_grad = True
+                logging.debug(f"Training parameter: {name}")
+
+        # Log trainable parameters
+        trainable_params = sum(p.numel() for p in self.model.parameters() if p.requires_grad)
+        total_params = sum(p.numel() for p in self.model.parameters())
+        logging.info(
+            f"LoRA training: {trainable_params:,} trainable parameters out of {total_params:,} ({100 * trainable_params / total_params:.2f}%)"
+        )
+
     @classmethod
     def from_pretrained(
         cls: builtins.type[T],
@@ -1026,7 +1163,18 @@ class PI05Policy(PreTrainedPolicy):
         return fixed_state_dict
 
     def get_optim_params(self) -> dict:
-        return self.parameters()
+        """Get parameters for optimization, considering LoRA configuration."""
+        if getattr(self.config, "use_lora", False):
+            # Only return parameters that require gradients (LoRA parameters and projection layers)
+            trainable_params = []
+            for _, param in self.named_parameters():
+                if param.requires_grad:
+                    trainable_params.append(param)
+            logging.info(f"Total trainable parameters with LoRA: {len(trainable_params)}")
+            return trainable_params
+        else:
+            # Return all parameters for full fine-tuning
+            return self.parameters()
 
     def reset(self):
         """Reset internal state - called when environment resets."""
@@ -1137,7 +1285,7 @@ class PI05Policy(PreTrainedPolicy):
 
         return actions
 
-    def forward(self, batch: dict[str, Tensor]) -> tuple[Tensor, dict]:
+    def forward(self, batch: dict[str, Tensor], teacher_policy=None) -> tuple[Tensor, dict]:
         """Run the batch through the model and compute the loss for training."""
 
         # Prepare inputs
@@ -1156,7 +1304,7 @@ class PI05Policy(PreTrainedPolicy):
         loss = losses.mean()
 
         loss_dict = {
-            "loss": loss.item(),
+            "loss": losses,
             "loss_per_dim": losses.mean(dim=[0, 1]).detach().cpu().numpy().tolist(),
         }
 
diff --git a/src/lerobot/scripts/knowledge_distillation.py b/src/lerobot/scripts/knowledge_distillation.py
new file mode 100644
index 00000000..b471410b
--- /dev/null
+++ b/src/lerobot/scripts/knowledge_distillation.py
@@ -0,0 +1,73 @@
+import argparse
+import logging
+import sys
+from pprint import pformat
+
+import torch
+
+from lerobot.configs import parser
+from lerobot.configs.train import TrainPipelineConfig
+from lerobot.datasets.factory import make_dataset
+from lerobot.policies.factory import make_policy
+from lerobot.utils.random_utils import set_seed
+
+
+def get_teacher_model(model_path, dataset, bs):
+    temp = sys.argv[1:]
+    sys.argv[1:] = ["--config_path=" + model_path, "--resume=true", f"--batch_size={bs}"]
+    policy = compression()
+    sys.argv[1:] = temp
+    return policy
+
+
+@parser.wrap()
+def compression(cfg: TrainPipelineConfig, dataset=None):
+    cfg.validate()
+    logging.info(pformat(cfg.to_dict()))
+
+    if dataset is None:
+        dataset = make_dataset(cfg)
+    print("--------------------------------------------")
+    print(dataset.meta)
+    print("--------------------------------------------")
+
+    if cfg.seed is not None:
+        set_seed(cfg.seed)
+
+    # Check device is available
+    torch.backends.cudnn.benchmark = True
+    torch.backends.cuda.matmul.allow_tf32 = True
+
+    logging.info("Creating policy")
+    policy = make_policy(
+        cfg=cfg.policy,
+        ds_meta=dataset.meta,
+    )
+    policy.eval()
+    return policy
+
+
+def get_model(model_path, bs):
+    temp = sys.argv[1:]
+    sys.argv[1:] = ["--config_path=" + model_path, "--resume=true", f"--batch_size={bs}"]
+    policy = compression()
+    sys.argv[1:] = temp
+    return policy
+
+
+if __name__ == "__main__":
+    # 创建参数解析器
+    parser = argparse.ArgumentParser(
+        description="处理模型配置文件（必填模型路径参数）",
+        epilog="示例: python script.py --model_path=/path/to/train_config.json",
+    )
+    parser.add_argument("--model_path", type=str, help="教师模型训练配置文件路径")
+
+    # 解析参数
+    if len(sys.argv) == 1:
+        parser.print_help()
+        sys.exit(1)
+    args = parser.parse_args()
+    policy = get_model(args.model_path, 1)
+
+    print(policy)
diff --git a/src/lerobot/scripts/lerobot_train.py b/src/lerobot/scripts/lerobot_train.py
index 3b4ac207..ebc5b9c0 100644
--- a/src/lerobot/scripts/lerobot_train.py
+++ b/src/lerobot/scripts/lerobot_train.py
@@ -13,17 +13,20 @@
 # WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 # See the License for the specific language governing permissions and
 # limitations under the License.
+import copy
 import logging
+import os
 import time
 from contextlib import nullcontext
 from pprint import pformat
 from typing import Any
-from tqdm import tqdm
 
 import torch
 from accelerate import Accelerator
 from termcolor import colored
 from torch.optim import Optimizer
+from torch.utils.tensorboard import SummaryWriter
+from tqdm import tqdm
 
 from lerobot.configs import parser
 from lerobot.configs.train import TrainPipelineConfig
@@ -36,17 +39,18 @@ from lerobot.optim.factory import make_optimizer_and_scheduler
 from lerobot.policies.factory import make_policy, make_pre_post_processors
 from lerobot.policies.pretrained import PreTrainedPolicy
 from lerobot.rl.wandb_utils import WandBLogger
+from lerobot.scripts.knowledge_distillation import get_teacher_model
 from lerobot.scripts.lerobot_eval import eval_policy_all
 from lerobot.utils.logging_utils import AverageMeter, MetricsTracker
+from lerobot.utils.qat import QuantConstraint
 from lerobot.utils.random_utils import set_seed
 from lerobot.utils.train_utils import (
+    WeightSampler,
     get_step_checkpoint_dir,
     get_step_identifier,
     load_training_state,
     save_checkpoint,
     update_last_checkpoint,
-    WeightSampler,
-    normalize_weights,
 )
 from lerobot.utils.utils import (
     format_big_number,
@@ -54,6 +58,7 @@ from lerobot.utils.utils import (
     init_logging,
 )
 
+
 def update_policy(
     train_metrics: MetricsTracker,
     policy: PreTrainedPolicy,
@@ -62,6 +67,7 @@ def update_policy(
     grad_clip_norm: float,
     accelerator: Accelerator,
     lr_scheduler=None,
+    teacher_policy=None,
     lock=None,
 ) -> tuple[MetricsTracker, dict]:
     """
@@ -89,12 +95,28 @@ def update_policy(
     policy.train()
 
     # Let accelerator handle mixed precision
-    with accelerator.autocast():
-        loss, output_dict = policy.forward(batch)
+    teacher_feature = None
+    if teacher_policy:
+        if policy.type == "dp":
+            with accelerator.autocast():
+                teacher_feature = teacher_policy.forward(
+                    batch, teacher_policy=None, teacher_feature=None, get_feature=True
+                )
+        else:
+            with accelerator.autocast():
+                loss, output_dict = policy.forward(
+                    batch, teacher_policy=teacher_policy, teacher_feature=teacher_feature
+                )
         # TODO(rcadene): policy.unnormalize_outputs(out_dict)
+    else:
+        with accelerator.autocast():
+            loss, output_dict = policy.forward(batch)
 
     # Use accelerator's backward method
-    accelerator.backward(loss)
+    if teacher_policy:
+        accelerator.backward(output_dict["total_kd_loss"])
+    else:
+        accelerator.backward(loss)
 
     # Clip gradients if specified
     if grad_clip_norm > 0:
@@ -119,12 +141,49 @@ def update_policy(
         accelerator.unwrap_model(policy, keep_fp32_wrapper=True).update()
 
     train_metrics.loss = loss.item()
+    if teacher_policy:
+        train_metrics.total_kd_loss = (
+            output_dict.get("total_kd_loss", 0.0) if "total_kd_loss" in output_dict else 0.0
+        )
     train_metrics.grad_norm = grad_norm.item()
     train_metrics.lr = optimizer.param_groups[0]["lr"]
     train_metrics.update_s = time.perf_counter() - start_time
     return train_metrics, output_dict
 
 
+def set_train_sample_weights(cfg, dataset, train_dataset, train_num_samples):
+    # 初始化样本权重（初始值均为1.0），初始化样本计数器为0
+    train_sample_weights = [1.0] * train_num_samples
+    if not cfg.custom_weights:
+        return train_sample_weights
+    if hasattr(dataset, "hf_dataset") and "training_weight" in dataset.hf_dataset.features:
+        logging.info("Found 'training_weight' in dataset. Extracting custom weights...")
+        # 直接获取整列权重数据
+        full_weights_column = dataset.hf_dataset["training_weight"]
+
+        # 根据 train_dataset 的索引映射提取对应的权重
+        # train_dataset.indices 存储了该子集在原始数据集中的索引
+        train_sample_weights = [float(full_weights_column[i]) for i in train_dataset.indices]
+
+        logging.info(f"Successfully loaded {len(train_sample_weights)} custom weights.")
+
+        if len(train_sample_weights) > 0:
+            logging.info(f"Weight stats - Max: {max(train_sample_weights)}, Min: {min(train_sample_weights)}")
+
+    else:
+        # 如果没有 hf_dataset 属性或者没有该列，尝试逐个访问
+        # 只有当确认 dataset[0] 包含该 key 时才执行，避免报错
+        if len(dataset) > 0 and "training_weight" in dataset[0]:
+            logging.info("Extracting 'training_weight' by iterating (slow path)...")
+            train_sample_weights = [
+                float(dataset[i]["training_weight"])
+                for i in tqdm(train_dataset.indices, desc="Loading Weights")
+            ]
+        else:
+            logging.warning("'training_weight' not found in dataset features. Using default 1.0 weights.")
+    return train_sample_weights
+
+
 @parser.wrap()
 def train(cfg: TrainPipelineConfig, accelerator: Accelerator | None = None):
     """
@@ -154,6 +213,16 @@ def train(cfg: TrainPipelineConfig, accelerator: Accelerator | None = None):
         ddp_kwargs = DistributedDataParallelKwargs(find_unused_parameters=True)
         accelerator = Accelerator(step_scheduler_with_optimizer=False, kwargs_handlers=[ddp_kwargs])
 
+    # 创建log和tensorboard保存目录
+    folder_path = "./logs"
+
+    # 检查文件夹是否已存在，避免报错
+    if not os.path.exists(folder_path):
+        os.mkdir(folder_path)
+        print(f"Logs folder {folder_path} created successfully")
+    else:
+        print(f"Logs folder {folder_path} already exists")
+
     init_logging(accelerator=accelerator)
 
     # Determine if this is the main process (for logging and checkpointing)
@@ -200,6 +269,16 @@ def train(cfg: TrainPipelineConfig, accelerator: Accelerator | None = None):
             logging.info("Creating env")
         eval_env = make_env(cfg.env, n_envs=cfg.eval.batch_size, use_async_envs=cfg.eval.use_async_envs)
 
+    # Create teacher model for knowledge distillation
+    if cfg.policy.kd:
+        cfg_copy = copy.deepcopy(cfg)
+        model_path = cfg.policy.teacher_train_config
+        teacher_policy = get_teacher_model(model_path, dataset, cfg.batch_size)
+        cfg = cfg_copy
+        teacher_policy.eval()
+    else:
+        teacher_policy = None
+
     if is_main_process:
         logging.info("Creating policy")
     policy = make_policy(
@@ -208,6 +287,14 @@ def train(cfg: TrainPipelineConfig, accelerator: Accelerator | None = None):
         rename_map=cfg.rename_map,
     )
 
+    quant_constraint = QuantConstraint(
+        policy=policy,  # 传入完整policy，用于解包分布式封装
+        accelerator=accelerator,
+        quant_param_min=cfg.clipping_min,
+        quant_param_max=cfg.clipping_max,
+        hisi_log_path="./haisi.log",
+    )
+
     # Wait for all processes to finish policy creation before continuing
     accelerator.wait_for_everyone()
 
@@ -257,6 +344,9 @@ def train(cfg: TrainPipelineConfig, accelerator: Accelerator | None = None):
     num_learnable_params = sum(p.numel() for p in policy.parameters() if p.requires_grad)
     num_total_params = sum(p.numel() for p in policy.parameters())
 
+    log_dir = f"logs/pytorch/{str(cfg.output_dir).split('/')[-1]}"
+    writer = SummaryWriter(log_dir=log_dir)
+
     if is_main_process:
         logging.info(colored("Output dir:", "yellow", attrs=["bold"]) + f" {cfg.output_dir}")
         if cfg.env is not None:
@@ -272,7 +362,6 @@ def train(cfg: TrainPipelineConfig, accelerator: Accelerator | None = None):
 
     # create dataloader for offline training
     if hasattr(cfg.policy, "drop_n_last_frames"):
-        shuffle = False
         sampler = EpisodeAwareSampler(
             dataset.meta.episodes["dataset_from_index"],
             dataset.meta.episodes["dataset_to_index"],
@@ -280,15 +369,14 @@ def train(cfg: TrainPipelineConfig, accelerator: Accelerator | None = None):
             shuffle=True,
         )
     else:
-        shuffle = True
         sampler = None
 
     # 将整理数据集划分为 90% 作为训练集, 10% 作为验证集
-    train_size = int(0.9 * len(dataset)) # 计算训练集样本数量
-    val_size = len(dataset) - train_size # 计算验证集样本数量
+    train_size = int(0.9 * len(dataset))  # 计算训练集样本数量
+    val_size = len(dataset) - train_size  # 计算验证集样本数量
     logging.info(f"train_data_num: {train_size}, eval_data_num: {val_size}")
     train_dataset, val_dataset = torch.utils.data.random_split(dataset, [train_size, val_size])
-    
+
     # 如果 train_dataset 是 Subset（random_split 返回的是 Subset），
     # 保存原始数据集索引 -> 子集内位置的映射，用于后面根据 batch 返回的原始索引更新权重。
     try:
@@ -301,16 +389,11 @@ def train(cfg: TrainPipelineConfig, accelerator: Accelerator | None = None):
     train_num_samples = len(train_dataset)
     print(f"train_num_samples: {train_num_samples}")
     # 初始化样本权重（初始值均为1.0），初始化样本计数器为0
-    train_sample_weights = [1.0] * train_num_samples
+    train_sample_weights = set_train_sample_weights(cfg, dataset, train_dataset, train_num_samples)
     train_sample_count = [0] * train_num_samples
 
     # 划分训练和验证集
-    if cfg.ada_weight:
-        sampler = WeightSampler(
-            weights=train_sample_weights,
-            num_samples=train_num_samples,
-            replacement=True
-        )
+    sampler = WeightSampler(weights=train_sample_weights, num_samples=train_num_samples, replacement=True)
     train_loader = torch.utils.data.DataLoader(
         train_dataset,
         num_workers=cfg.num_workers,
@@ -318,7 +401,7 @@ def train(cfg: TrainPipelineConfig, accelerator: Accelerator | None = None):
         sampler=sampler,
         shuffle=False,
         pin_memory=device.type != "cpu",
-        drop_last=False
+        drop_last=False,
     )
 
     # 验证集不需要设置权重与不均衡采样
@@ -336,7 +419,6 @@ def train(cfg: TrainPipelineConfig, accelerator: Accelerator | None = None):
     policy, optimizer, train_loader, lr_scheduler = accelerator.prepare(
         policy, optimizer, train_loader, lr_scheduler
     )
-    val_loader = accelerator.prepare(val_loader)
     train_iter = cycle(train_loader)
     val_iter = cycle(val_loader)
 
@@ -350,6 +432,9 @@ def train(cfg: TrainPipelineConfig, accelerator: Accelerator | None = None):
         "dataloading_s": AverageMeter("data_s", ":.3f"),
     }
 
+    if cfg.policy.kd:
+        train_metrics["total_kd_loss"] = AverageMeter("total_kd_loss", ":.3f")
+
     # Use effective batch size for proper epoch calculation in distributed training
     effective_batch_size = cfg.batch_size * accelerator.num_processes
     train_tracker = MetricsTracker(
@@ -375,9 +460,7 @@ def train(cfg: TrainPipelineConfig, accelerator: Accelerator | None = None):
             # train_sample_weights = normalize_weights(train_sample_weights)
             # 根据样本权重更新采样器
             sampler = WeightSampler(
-                weights=train_sample_weights,
-                num_samples=train_num_samples,
-                replacement=True
+                weights=train_sample_weights, num_samples=train_num_samples, replacement=True
             )
             # 在更新采样器后，重新创建 DataLoader 和迭代器
             train_loader = torch.utils.data.DataLoader(
@@ -387,7 +470,7 @@ def train(cfg: TrainPipelineConfig, accelerator: Accelerator | None = None):
                 sampler=sampler,
                 shuffle=False,
                 pin_memory=device.type != "cpu",
-                drop_last=False
+                drop_last=False,
             )
             train_iter = cycle(train_loader)
 
@@ -403,12 +486,14 @@ def train(cfg: TrainPipelineConfig, accelerator: Accelerator | None = None):
             cfg.optimizer.grad_clip_norm,
             accelerator=accelerator,
             lr_scheduler=lr_scheduler,
+            teacher_policy=teacher_policy,
         )
 
+        # 关键：根据当前batch的损失更新样本权重
         if cfg.ada_weight:
-            # 关键：根据当前batch的损失更新样本权重
-            # 策略：损失越大的样本，权重越高（可根据需求修改策略，目前计算公式为 new_weight = old_weight * 0.9 + loss）
-            loss_mean = output_dict['loss'].mean(dim=[1, 2])
+            # 策略：损失越大的样本，权重越高（可根据需求修改策略，目前计算公式为
+            # new_weight = old_weight * 0.9 + loss）
+            loss_mean = output_dict["loss"].mean(dim=[1, 2])
             for index, raw_idx in enumerate(batch_indices):
                 # batch_indices 可能是 tensor
                 idx_val = int(raw_idx)
@@ -425,8 +510,13 @@ def train(cfg: TrainPipelineConfig, accelerator: Accelerator | None = None):
                         # 未在子集里（不应该发生），跳过
                         continue
 
-                # 权重更新公式：新权重 = 旧权重 * 0.9 + 损失值（示例）
-                train_sample_weights[subset_idx] = train_sample_weights[subset_idx] * cfg.damping_coefficient + float(loss_mean[index].item()) * 10
+                # 权重更新公式：新权重 = 旧权重 * damping_coefficient + 损失值 * 10
+                if train_sample_weights[subset_idx] == 0:
+                    continue
+                train_sample_weights[subset_idx] = (
+                    train_sample_weights[subset_idx] * cfg.damping_coefficient
+                    + float(loss_mean[index].item()) * 10
+                )
                 # 限制权重最小值，避免权重过小导致永远不被选中
                 train_sample_weights[subset_idx] = max(train_sample_weights[subset_idx], 0.1)
                 # 记录样本被选中的次数
@@ -434,6 +524,8 @@ def train(cfg: TrainPipelineConfig, accelerator: Accelerator | None = None):
 
         # Note: eval and checkpoint happens *after* the `step`th training update has completed, so we
         # increment `step` here.
+        if cfg.weight_clipping:
+            quant_constraint.clip_weights()
         step += 1
         train_tracker.step()
         is_log_step = cfg.log_freq > 0 and step % cfg.log_freq == 0 and is_main_process
@@ -442,6 +534,13 @@ def train(cfg: TrainPipelineConfig, accelerator: Accelerator | None = None):
 
         if is_log_step:
             logging.info(train_tracker)
+            # 写入tensorboard
+            for m in train_tracker.metrics.values():
+                m = str(m)
+                v = float(m.split(":")[-1])
+                n = m.split(":")[0]
+                writer.add_scalar(f"{n}/step", v, step)
+
             if wandb_logger:
                 wandb_log_dict = train_tracker.to_dict()
                 if output_dict:
@@ -451,28 +550,32 @@ def train(cfg: TrainPipelineConfig, accelerator: Accelerator | None = None):
 
         best_eval_sign = False
         if not cfg.env and is_eval_step:
+            torch.cuda.empty_cache()
             policy.eval()
             step_id = get_step_identifier(step, cfg.steps)
-            
-            per_eval_num = len(val_loader) // 4
+            # 减少验证集的评估批次数量，避免过长时间的评估
+            per_eval_num = len(val_loader) // 4 + 1
             logging.info(f"Start evaluating: {per_eval_num}/{len(val_loader)} batches")
             eval_loss = 0.0
-            for i in tqdm(range(per_eval_num)):
-                val_batch, _ = next(val_iter)
-                val_batch = preprocessor(val_batch)
-                with accelerator.autocast():
+            with torch.no_grad():  # 禁止梯度计算，不存储中间激活的梯度信息
+                for _ in tqdm(range(per_eval_num)):
+                    val_batch, _ = next(val_iter)
+                    val_batch = preprocessor(val_batch)
                     loss, output_dict = policy.forward(val_batch)
-                eval_loss += loss.item()
+                    eval_loss += loss.item()
+                torch.cuda.empty_cache()
             eval_loss /= per_eval_num
             logging.info(f"Eval policy at step {step}, eval_loss: {eval_loss:.3f}")
+            if cfg.ada_weight:
+                logging.info(train_sample_weights[:1000])
+                logging.info(train_sample_count[:1000])
+            writer.add_scalar("Loss/eval", eval_loss, step)
 
             if eval_loss < best_eval_loss:
                 best_eval_loss = eval_loss
                 logging.info(f"New best eval loss: {best_eval_loss:.3f}, saving checkpoint")
                 best_eval_sign = True
 
-            torch.cuda.empty_cache()
-
         if (cfg.save_checkpoint and is_saving_step) or best_eval_sign:
             if is_main_process:
                 logging.info(f"Checkpoint policy after step {step}")
-- 
2.34.1

